<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gêneros</title>
    <link rel="stylesheet" href="/genero.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <link rel="shortcut icon" href="images/logo-favico.png" type="image/x-icon">
</head>
<body>
    <header>
        <nav>
            <a href="/">Início</a>
            <a href="/artistas">Artistas</a>
            <a href="/discos">Discos</a>
            <a href="/faixas">Faixas</a>
            <a href="/generos">Gêneros</a>
        </nav>
    </header>

    <div class="generos-container">
        <a href="/generos/add" class="add-genre-button"><i class="fas fa-plus-circle"></i> Adicionar Gênero</a>

        <div class="generos-list">
            <% generos.forEach(function(genero) { %>
                <div class="genero-item">
                    <button class="genero-name" onclick="window.location.href='/generos/<%= genero.id %>'">
                        <%= genero.nome %>
                    </button>
                    <div class="genero-options">
                        <button class="edit-genre" onclick="window.location.href='/generos/<%= genero.id %>/edit'">
                            <i class="fas fa-edit"></i> Editar
                        </button>

                        <form action="/generos/<%= genero.id %>" method="POST" onsubmit="return confirm('Você tem certeza que deseja excluir este gênero?');">
                            <input type="hidden" name="_method" value="DELETE">
                            <button type="submit" class="delete-genre">
                                <i class="fas fa-trash"></i> Excluir
                            </button>
                        </form>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>
